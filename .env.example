# PostgreSQL (Neon) — use pooled URL for the app, direct URL for migrations / ETL
DATABASE_URL=
DIRECT_DATABASE_URL=

# Legacy Mongo (data migration script only; remove after cutover)
MONGO_DATABASE_URL=

# BETTER-AUTH
# Dev: https://tku-sparring.localhost  |  Prod: https://tku-sparring.vercel.app
BETTER_AUTH_URL=https://tku-sparring.localhost
BETTER_AUTH_SECRET=

# --- Tournament realtime (main app → external Socket.io service) ---
# Server-side POST. Node cannot resolve *.localhost on Windows; use 127.0.0.1 (or localhost).
REALTIME_INTERNAL_BROADCAST_URL=http://127.0.0.1:3331/internal/broadcast
REALTIME_INTERNAL_BROADCAST_SECRET=dev-shared-secret
TOURNAMENT_SOCKET_JWT_SECRET=dev-jwt-secret-at-least-32-chars-recommended
# Browser socket. Under Portless: https://ws.tss.localhost
VITE_REALTIME_URL=https://ws.tss.localhost
